-*- Mode: Diff -*-
--- glew/Makefile
+++ glew/Makefile
@@ -65,7 +65,7 @@
 RM      ?= rm -f
 LN      ?= ln -sf
 
-ifeq ($(MAKECMDGOALS), debug)
+ifneq (,$(filter debug,$(MAKECMDGOALS)))
 OPT = -g
 else
 OPT = $(POPT)
