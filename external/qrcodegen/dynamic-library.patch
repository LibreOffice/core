diff --git a/cpp/Makefile b/cpp/Makefile
index 858a1bf..0019c6f 100644
--- a/cpp/Makefile
+++ b/cpp/Makefile
@@ -53,28 +53,33 @@ CXXFLAGS += -O1
 
 LIB = qrcodegen
 LIBFILE = lib$(LIB).a
+LIBFILE_SHARED = lib$(LIB).so
 LIBOBJ = BitBuffer.o QrCode.o QrSegment.o
 MAINS = QrCodeGeneratorDemo QrCodeGeneratorWorker
 
 # Build all binaries
-all: $(LIBFILE) $(MAINS)
+all: $(LIBFILE_SHARED) $(MAINS) $(LIBFILE)
 
 # Delete build output
 clean:
-	rm -f -- $(LIBOBJ) $(LIBFILE) $(MAINS:=.o) $(MAINS)
+	rm -f -- $(LIBOBJ) $(LIBFILE) $(MAINS:=.o) $(MAINS) $(LIBFILE_SHARED)
 	rm -rf .deps
 
 # Executable files
-%: %.o $(LIBFILE)
+%: %.o $(LIBFILE_SHARED)
 	$(CXX) $(CXXFLAGS) -o $@ $< -L . -l $(LIB)
 
+# .so Library
+$(LIBFILE_SHARED): $(LIBOBJ)
+	$(CXX) $(LIBOBJ) -shared -o $(LIBFILE_SHARED)
+
 # The library
 $(LIBFILE): $(LIBOBJ)
 	$(AR) -crs $@ -- $^
 
 # Object files
 %.o: %.cpp .deps/timestamp
-	$(CXX) $(CXXFLAGS) -c -o $@ -MMD -MF .deps/$*.d $<
+	$(CXX) $(CXXFLAGS) -c -fPIC -o $@ -MMD -MF .deps/$*.d $<
 
 # Have a place to store header dependencies automatically generated by compiler
 .deps/timestamp:
