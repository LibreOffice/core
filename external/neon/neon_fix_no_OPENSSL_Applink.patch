--- src/ne_openssl.c
+++ src/ne_openssl.c
@@ -440,7 +440,7 @@
 #ifdef NE_DEBUGGING
         if (ne_debug_mask & NE_DBG_SSL) {
             fprintf(ne_debug_stream, "Cert #%d:\n", n);
-            X509_print_fp(ne_debug_stream, cert->subject);
+            // X509_print_fp(ne_debug_stream, cert->subject);
         }
 #endif
         if (top == NULL) {
