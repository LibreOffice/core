
if ( ! -f $SRC_ROOT/dmake/dmake ) then

cd $SRC_ROOT/dmake
if "$?" != "0" exit $?

./configure
if "$?" != "0" exit $?

make
if "$?" != "0" exit $?

echo ""
echo "dmake as been successfully built"

cd ..

endif

